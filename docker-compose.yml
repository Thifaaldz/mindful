services:
  php:
    build:
      context: ./php
      extra_hosts:
        - "pecl.php.net=156.59.92.126"
    container_name: mindfulledu_php
    volumes:
      - ./src:/var/www/html
    environment:
      - PROJECT_NAME=mindfulledu
      - MINDFULEDU_ML_URL=http://ml:8000
    depends_on:
      - db
      - ml
    healthcheck:
      test: ["CMD", "composer", "--version"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    build:
      context: ./nginx
    container_name: mindfulledu_nginx
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./src:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - php
    healthcheck:
      test: ["CMD-SHELL", "curl --silent --show-error --fail --insecure https://127.0.0.1/up || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  db:
    image: mariadb:10.11
    container_name: mindfulledu_db
    ports:
      - "13306:3306"
    environment:
      MYSQL_DATABASE: mindfulledu
      MYSQL_ROOT_PASSWORD: p455w0rd
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d
      - ./db/data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  ml:
    build:
      context: ./ml-service
    container_name: mindfulledu_ml
    env_file:
      - ./ml-service/.env
    ports:
      - "18000:8000"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')\""]
      interval: 30s
      timeout: 10s
      retries: 3
